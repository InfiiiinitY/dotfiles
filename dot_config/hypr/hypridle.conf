general {
    lock_cmd = pidof hyprlock || hyprlock       # avoid starting multiple hyprlock instances.
    before_sleep_cmd = loginctl lock-session    # lock before suspend.
    after_sleep_cmd = hyprctl dispatch "hl.dsp.dpms({ action = 'on' })"  # to avoid having to press a key twice to turn on the display.
}

listener {
    timeout = 180                                 # 3min
    on-timeout = notify-send -u low -t 10000 -n "/usr/share/icons/breeze-dark/actions/22/system-hibernate.svg" "Hypridle" "Are you still there?<br><b>Locking screen in 2 Minutes!</b>"
}

listener {
    timeout = 300                                 # 5min
    on-timeout = loginctl lock-session            # lock screen when timeout has passed
}

listener {
    timeout = 600                                 # 10min
    on-timeout = hyprctl dispatch "hl.dsp.dpms({ action = 'off' })" # screen off when timeout has passed
    on-resume = hyprctl dispatch "hl.dsp.dpms({ action = 'on' })"   # screen on when activity is detected after timeout has fired.
}

# Having problems with Nvidia and resuming from suspend - Disabling this for now!
# listener {
#     timeout = 1800                                # 30min
#     on-timeout = systemctl suspend                # suspend pc
# }
